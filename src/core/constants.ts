export enum Url {
    home = "/",
    details = "details",
    detailsName = "name",
}

export enum Api {
    baseURL = "https://restcountries.com/v3.1/",
    allCountries = "all",
    singleCountry = "name",
    countriesNamesByCode = "alpha",
}